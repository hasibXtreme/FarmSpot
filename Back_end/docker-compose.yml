

services:
  postgres-db:
    image: postgres:16-alpine
    container_name: farm_db_container
    restart: always
    environment:
      POSTGRES_USER: Hasib
      POSTGRES_PASSWORD: 69420
      POSTGRES_DB: farm_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: